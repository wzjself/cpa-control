- 用户今天确认：GitHub 项目 `router-for-me/CLIProxyAPI` 已部署到 `/opt/CLIProxyAPI-8318`，容器名 `cli-proxy-api-8318`，对外端口是 `8318`（不是 `8138`）。
- 已核实 `8318` 当前正常监听在 `0.0.0.0:8318`，本机访问 `/` 返回 200，本机访问后台页路径 `/management.html` 可返回页面内容；若外网打不开，优先排查防火墙/安全组而不是服务本身未启动。
- 该 CPA 实例的管理密钥在 `config.yaml` 中保存为 bcrypt 哈希，无法从配置反推出原始后台密码；如需登录后台，通常需要用户提供原密码或直接重置新密码。
- 已在服务器部署新的 `router-for-me/CLIProxyAPI` 实例到 `/opt/CLIProxyAPI-8319`，容器名 `cli-proxy-api-8319`，对外端口 `8319`；当前本机访问 `/` 正常返回 200。
